import { View, Text } from "react-native";

/*** Once project is complete please rename the file to [...missing].tsx this will disable typed routs as al invalid routes com here */

interface Props {}

export default function MissingScreen({}: Props) {
  return (
    <View>
      <Text>Page Not Found</Text>
    </View>
  );
}
